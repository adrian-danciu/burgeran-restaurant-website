import { AboutBanner, Hero, TopProducts } from "../components";

const Home = () => {
  return (
    <div>
      <Hero />
      <TopProducts />
      <AboutBanner />
    </div>
  );
};

export default Home;
